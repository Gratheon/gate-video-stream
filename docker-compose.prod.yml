services:
  gate-video-stream:
    build:
      context: . 
      dockerfile: ./Dockerfile.prod
    env_file:
      - ./config/config.prod.env
    environment:
      - ENV_ID=prod
      - PORT=8900
    ports:
      - 8900:8900
      - 8950:8950
    restart: always
    network_mode: host
