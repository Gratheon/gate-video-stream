name: deploy
on:
  push:
    branches: main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: pull
        working-directory: /www/gate-video-stream/
        run: git reset --hard && git pull

      - name: restart
        working-directory: /www/gate-video-stream/
        run: chmod +x restart.sh && ./restart.sh

      - name: Verify production deployment
        working-directory: /www/gate-video-stream/
        run: ./scripts/verify-production-deployment.sh


      - name: Verify production deployment
        working-directory: /www/gate-video-stream/
        run: chmod +x scripts/verify-production-deployment.sh && ./scripts/verify-production-deployment.sh